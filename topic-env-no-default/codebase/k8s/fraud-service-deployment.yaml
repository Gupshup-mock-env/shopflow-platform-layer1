apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: shopflow
  labels:
    app.kubernetes.io/name: fraud-service
    app.kubernetes.io/part-of: shopflow
    shopflow.io/domain: risk
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fraud-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fraud-service
        app.kubernetes.io/part-of: shopflow
    spec:
      containers:
        - name: fraud-service
          image: registry.shopflow.internal/risk/fraud-service:1.8.3
          envFrom:
            - configMapRef:
                name: fraud-messaging-config
          env:
            - name: SERVICE_NAME
              value: "fraud-service"
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "96Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
